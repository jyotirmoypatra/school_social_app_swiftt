# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CampusCircle' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CampusCircle
    pod 'SDWebImage'
    pod 'Firebase/Core'
    pod 'Firebase/Messaging'
  target 'CampusCircleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CampusCircleUITests' do
    # Pods for testing
  end

end
